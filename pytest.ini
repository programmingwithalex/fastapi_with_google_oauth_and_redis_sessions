[pytest]
# put your code dirs on sys.path so that:
#  - `import src.web.app` works (src package)
#  - `import logging_config` works inside each service
pythonpath =
    src
    src/auth
    src/web